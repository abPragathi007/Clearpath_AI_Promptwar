# ClearPath AI

A Vite + React frontend with an Express API for the ClearPath AI safety-routing concept.

## Development

```bash
npm install
npm run dev
```

The frontend runs on Vite and proxies `/api` calls to the backend on port `3001`.

## Build

```bash
npm run build
npm run start
```

The production server serves the built client from `dist/client`.
